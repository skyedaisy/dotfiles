#fastfetch
# Source external config files
[[ -f ${ZDOTDIR:-$HOME}/.zaliases ]] && source ${ZDOTDIR:-$HOME}/.zplugins
[[ -f ${ZDOTDIR:-$HOME}/.zaliases ]] && source ${ZDOTDIR:-$HOME}/.zaliases
[[ -f ${ZDOTDIR:-$HOME}/.zaliases ]] && source ${ZDOTDIR:-$HOME}/.zoptions
[[ -f ${ZDOTDIR:-$HOME}/.zaliases ]] && source ${ZDOTDIR:-$HOME}/.zfunctions

# Atuin
source "$HOME/.atuin/bin/env"
eval "$(atuin init zsh --disable-up-arrow)"

# Starship
eval "$(starship init zsh)"
